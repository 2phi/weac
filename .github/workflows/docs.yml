name: Build and publish Sphinx 🪬 documentation

on:
  push:
    branches:
      - main
    paths:
      - 'weac/**'
      - 'docs/sphinx/**'
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build and publish Sphinx 🪬 documentation
    runs-on: ubuntu-latest
    environment: docs-deploy

    steps:
    - name: Check out the repository
      uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install sphinx sphinxawesome_theme IPython
        pip install -e .

    - name: Build documentation
      run: |
        # Generate API docs
        sphinx-apidoc -o docs/sphinx/ weac --force --separate

        # Build HTML docs
        make -C docs/sphinx clean html

    - name: Deploy documentation
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: |
        set -euo pipefail
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"

        # Prepare a clean worktree for the documentation branch
        rm -rf docs-deploy
        git worktree add --force --detach docs-deploy
        pushd docs-deploy
        git checkout --orphan documentation || git checkout documentation
        git rm -rf . || true
        popd

        # Copy built HTML to the worktree root
        rsync -av --delete docs/sphinx/_build/html/ docs-deploy/
        touch docs-deploy/.nojekyll

        # Commit and push
        pushd docs-deploy
        git add -A
        if git diff --cached --quiet; then
          echo "No changes to publish"
        else
          git commit -m "Update documentation"
          git push -f origin HEAD:documentation
        fi
        popd
